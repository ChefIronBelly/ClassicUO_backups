# Timewarp

clearsysmsg

# Delay between move and says
setvar movDelay 1200
setvar sayDelay1 3000
setvar sayDelay2 4000

say 'Its astounding' 55
    wait sayDelay1
say 'Time is fleeting' 55
    wait sayDelay1
say 'Madness takes its toll' 55
    wait sayDelay1
say 'But listen closely' 55
    wait sayDelay2
say 'Lets do the Time Warp again' 55
    wait sayDelay2
say 'Lets do the T1ime Warp again' 55
    wait sayDelay2
say 'Its just a jump to the left' 55
    walk 'right'
    wait 3000
say 'And then a step to the right' 55
    walk 'left'
    wait 3000
    walk 'south'
    wait movDelay   
say 'With your hands on your hips' 55
    wait sayDelay1
say 'You bring your knees in tight' 55
    wait sayDelay1
say 'But its the pelvic thrust' 55        
    wait sayDelay1
say 'That really drives you insane' 55
    wait sayDelay2
say 'Lets do the Time Warp again' 55
    wait sayDelay2
say 'Lets do the Time Warp again' 55
    wait sayDelay2
say 'Its just a jump to the left' 55
    walk 'right'
    wait 3000
say 'And then a step to the right' 55
    walk 'left'
    wait 3000
    walk 'south'
    wait movDelay