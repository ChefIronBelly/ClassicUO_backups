if insysmsg 'What instrument'
    if findtype '3763' true
    usetype '3763'
    else 
        overhead 'No Loot!'
        stop
    endif
endif
if skill 'peacemakeing' < 100
    useskill 'peacemaking'
    wft
    target 'self'
    elseif skill 'musicianship' < 100
    usetype '10245'
endif
wait 10000
loop